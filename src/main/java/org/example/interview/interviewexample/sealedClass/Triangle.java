package org.example.interview.interviewexample.sealedClass;

// Non-sealed class example: Triangle can extend Square
public class Triangle extends Square {
    public Triangle(double side) {
        super(side);
    }
}
